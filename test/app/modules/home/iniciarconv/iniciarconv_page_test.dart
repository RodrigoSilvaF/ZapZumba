import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:projeto_intensivao/app/modules/home/iniciarconv/iniciarconv_page.dart';

void main() {
  testWidgets('IniciarconvPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(IniciarconvPage(title: 'Iniciarconv')));
    //  final titleFinder = find.text('Iniciarconv');
    //  expect(titleFinder, findsOneWidget);
  });
}
