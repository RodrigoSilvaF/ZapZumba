import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:projeto_intensivao/app/modules/home/iniciarconv/iniciarconv_controller.dart';
import 'package:projeto_intensivao/app/modules/home/iniciarconv/iniciarconv_module.dart';

void main() {
  initModule(IniciarconvModule());
  // IniciarconvController iniciarconv;
  //
  setUp(() {
    //     iniciarconv = IniciarconvModule.to.get<IniciarconvController>();
  });

  group('IniciarconvController Test', () {
    //   test("First Test", () {
    //     expect(iniciarconv, isInstanceOf<IniciarconvController>());
    //   });

    //   test("Set Value", () {
    //     expect(iniciarconv.value, equals(0));
    //     iniciarconv.increment();
    //     expect(iniciarconv.value, equals(1));
    //   });
  });
}
