import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:projeto_intensivao/app/modules/home/conversas/conversas_page.dart';

void main() {
  testWidgets('ConversasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ConversasPage(title: 'Conversas')));
    //  final titleFinder = find.text('Conversas');
    //  expect(titleFinder, findsOneWidget);
  });
}
