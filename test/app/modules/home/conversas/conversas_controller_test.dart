import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:projeto_intensivao/app/modules/home/conversas/conversas_controller.dart';
import 'package:projeto_intensivao/app/modules/home/conversas/conversas_module.dart';

void main() {
  initModule(ConversasModule());
  // ConversasController conversas;
  //
  setUp(() {
    //     conversas = ConversasModule.to.get<ConversasController>();
  });

  group('ConversasController Test', () {
    //   test("First Test", () {
    //     expect(conversas, isInstanceOf<ConversasController>());
    //   });

    //   test("Set Value", () {
    //     expect(conversas.value, equals(0));
    //     conversas.increment();
    //     expect(conversas.value, equals(1));
    //   });
  });
}
